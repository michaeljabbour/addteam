[project]
name = "addteam"
version = "0.1.0"
description = "One-command access + onboarding bootstrap for this repo."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "httpx",
    "rich",
]

[tool.ruff]
line-length = 120
target-version = "py310"
